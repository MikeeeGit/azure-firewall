# Changelog

## Unreleased

- Add an explicit, tested Envoy image/CSI vault egress profile for the maintained dual-AKS ingress example.

- Reconcile September source without importing estate allowlists; document non-circular DNS forwarding, independent upstream tests and rollback readiness.

- Create a provisional standalone public firewall component from the reviewed archived root.
- Retain base/child policy inheritance, ordered network/application rule groups, DNS, threat intelligence, non-SNAT ranges, zones and useful outputs using native AzureRM resources.
- Replace private estate identifiers/rules with layered synthetic UK South/West targets and explicit subscription/tenant binding.
- Add optional diagnostics, scoped AKS platform and Standard ACR egress, optional reciprocal cross-spoke HTTPS, and firewall outputs for a separately owned route add-on.
- Add credential-free mock tests, shared CI callers, setup/migration/provenance documentation and Apache-2.0 licensing.
- Explicitly reject unwired archived Basic/Virtual Hub options. This fresh-state public interface requires a separate reviewed migration for existing AVM resources.
